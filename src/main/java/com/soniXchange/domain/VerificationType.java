package com.soniXchange.domain;

/**
 * VerificationType
 *
 * Defines the methods available for verifying a user’s identity
 * within the SoniXchange platform (e.g., via MOBILE or EMAIL).
 */
public enum VerificationType {
  MOBILE,
  EMAIL
}

