spring.application.name=SoniXchange
server.port = 5454

#Hibernate automatically updates schema when entities change
spring.jpa.hibernate.ddl-auto=update

#Database connection
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/sonixchange
spring.datasource.username=soni
spring.datasource.password=password123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#Log SQL queries
#spring.jpa.show-sql: true

#Stripe & Razorpay Api Keys
stripe.api.key=${STRIPE_SECRET_KEY}

razorpay.api.key=fake razorpay api key 

razorpay.api.secret=fake razorpay api secret 